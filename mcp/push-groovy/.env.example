# Copy to .env and fill in your values, then export or source before running.
# Alternatively set these as system environment variables.

HUB_IP=192.168.0.142
APPLOADER_APP_ID=<installed_id_of_your_AppLoader_instance>
OAUTH_ACCESS_TOKEN=<access_token_shown_on_AppLoader_config_page>
